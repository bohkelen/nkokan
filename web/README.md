# `web/`

Frontend (PWA) package.

Planned responsibilities (Phase 1):

- offline-first dictionary lookup UX
- script toggle (Latin / N’Ko / side-by-side)
- local caching + client-side search index
- transparent sentence analysis UI (best-guess + breakdown + uncertainty markers)

No app code has been added yet.

